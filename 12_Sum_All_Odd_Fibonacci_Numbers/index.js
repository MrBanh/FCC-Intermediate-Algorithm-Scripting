function sumFibs(num) {
    // Set the base case
    let f0 = 0;
    let f1 = 1;
    let f2 = 1;

    // Array to store all odd fib numbers
    const oddFibs = [];

    // Loop up to the num
}

module.exports = sumFibs;